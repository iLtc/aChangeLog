<p id="notice"><%= notice %></p>

<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<!--<%= link_to 'Edit', '/' + @user.username + '/edit' %> |-->

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @apps.each do |app| %>
      <tr>
        <td><%= app.title %></td>
        <td><%= link_to 'Show', user_app_path(@user, app) %></td>
        <td><!--<%= link_to 'Edit', edit_user_app_path(@user, app) %>--></td>
        <td><!--<%= link_to 'Destroy', user_app_path(@user, app), method: :delete, data: { confirm: 'Are you sure?' } %>--></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New App', new_user_app_path(@user)  %> | <%= link_to 'Back', users_path %>
